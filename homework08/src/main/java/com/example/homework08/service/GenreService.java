package com.example.homework08.service;

/*
import com.example.homework08.exceptions.NotFoundException;
import com.example.homework08.model.Genre;
import lombok.RequiredArgsConstructor;
import org.springframework.stereotype.Service;

@Service
@RequiredArgsConstructor
public class GenreService {
    private final GenreRepository genreRepository;

    public Genre getGenreById(long id) {
        return genreRepository.findById(id).orElseThrow(() -> new NotFoundException("Жанр не " +
                "обнаружен!"));
    }

}
*/
